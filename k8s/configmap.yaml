apiVersion: v1
kind: ConfigMap
metadata:
  name: backend-config
  namespace: transport-app
data:
  PORT: "5000"
  NODE_ENV: "production"
  DB_HOST: "mongo-service"
  DB_PORT: "27017"
  DB_NAME: "transportdb"